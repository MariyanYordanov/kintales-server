// TODO: Implement in Feature 1.1 (Auth endpoints)
// - Google OAuth strategy (passport-google-oauth20)
// - JWT strategy for protected routes

/**
 * Configure Passport.js strategies.
 * @param {import('passport')} passport
 */
export function configurePassport(passport) {
  // Will be implemented in Feature 1.1
}
