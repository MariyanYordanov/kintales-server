/**
 * Seed test data for development.
 * Will be populated in later features.
 * @param {import('drizzle-orm/node-postgres').NodePgDatabase} db
 */
export async function seedTestData(db) {
  // TODO: Add test family tree with sample relatives in Feature 2.1
}
